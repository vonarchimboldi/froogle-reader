services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: writer_reader
    ports:
      - "5432:5432"
    volumes:
      - writer_reader_postgres:/var/lib/postgresql/data

volumes:
  writer_reader_postgres:
