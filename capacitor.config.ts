import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.paktochman.writerreader",
  appName: "Froogle Reader",
  webDir: "out"
};

export default config;
