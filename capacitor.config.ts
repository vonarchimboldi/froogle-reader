import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.paktochman.writerreader",
  appName: "Writer Reader",
  webDir: "out"
};

export default config;
